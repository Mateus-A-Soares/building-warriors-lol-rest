datasources:
  default:
    url: jdbc:h2:mem:lolDb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
    dialect: H2
jpa.default.properties.hibernate:
  hbm2ddl.auto: update
  show_sql: true
endpoints:
  health:
    enabled: true
    sensitive: false
    details-visible: NEVER